import { Component } from "@angular/core";

// ng new frontend --strict --minimal
@Component({
  selector: "app-root",
  templateUrl: "./app.component.html",
})
export class AppComponent {
  public name: string = "Ranyery";
}
